import{cookies}from"next/headers";import{redirect}from"next/navigation";import{AppShell}from"@/components/app-shell";import{StaffManagement,StaffRow}from"@/components/staff-management";import{getCurrentUser,serverGet}from"@/lib/api";
export default async function Page(){const cookie=(await cookies()).toString();const user=await getCurrentUser(cookie);if(!user)redirect("/login");if(user.role==="staff")redirect("/staff");if(user.role!=="owner")redirect("/admin/inventory");const rows=await serverGet<StaffRow[]>("/admin/staff",cookie);return <AppShell user={user} title="Staff management"><StaffManagement initial={rows}/></AppShell>}
