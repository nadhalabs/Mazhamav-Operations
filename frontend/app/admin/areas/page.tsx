import{cookies}from"next/headers";import{redirect}from"next/navigation";import{AppShell}from"@/components/app-shell";import{AreaPerformance}from"@/components/area-performance";import{getCurrentUser,serverGet}from"@/lib/api";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string>>}){const cookie=(await cookies()).toString(),user=await getCurrentUser(cookie);if(!user)redirect("/login");if(user.role!=="owner")redirect("/admin/inventory");const q=await searchParams,params=new URLSearchParams({period:q.period||"last_30_days"});if(q.date_from)params.set("date_from",q.date_from);if(q.date_to)params.set("date_to",q.date_to);const data=await serverGet<any>(`/areas?${params}`,cookie);return <AppShell user={user} title="Area performance"><AreaPerformance data={data}/></AppShell>}
