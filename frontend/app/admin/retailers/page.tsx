import{cookies}from"next/headers";import{redirect}from"next/navigation";import{AppShell}from"@/components/app-shell";import{RetailerManager}from"@/components/retailer-manager";import{getCurrentUser,serverGet}from"@/lib/api";
export default async function Page(){const cookie=(await cookies()).toString();const user=await getCurrentUser(cookie);if(!user)redirect("/login");if(user.role==="staff")redirect("/staff");const retailers=await serverGet<any[]>("/retailers?include_inactive=true",cookie);return <AppShell user={user} title="Retailers"><RetailerManager initial={retailers}/></AppShell>}
