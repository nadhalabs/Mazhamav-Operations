import{cookies}from"next/headers";import{redirect}from"next/navigation";import{AppShell}from"@/components/app-shell";import{ReportDownloads}from"@/components/report-downloads";import{getCurrentUser}from"@/lib/api";
export default async function ReportsPage(){const cookie=(await cookies()).toString();const user=await getCurrentUser(cookie);if(!user)redirect("/login");if(user.role!=="owner")redirect("/admin/inventory");return <AppShell user={user} title="Reports"><p className="mb-6 max-w-3xl text-sm text-slate-600">Download operational data as CSV for reconciliation, finance, or offline analysis. Select a date range before exporting.</p><ReportDownloads/></AppShell>}
