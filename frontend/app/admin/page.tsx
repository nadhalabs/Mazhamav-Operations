import{cookies}from"next/headers";import{redirect}from"next/navigation";import{AppShell}from"@/components/app-shell";import{OwnerDashboard}from"@/components/owner-dashboard";import{getCurrentUser,serverGet}from"@/lib/api";
export default async function AdminPage({searchParams}:{searchParams:Promise<Record<string,string>>}){const cookie=(await cookies()).toString();const user=await getCurrentUser(cookie);if(!user)redirect("/login");if(user.role==="staff")redirect("/staff");if(user.role==="manager")redirect("/admin/inventory");const params=await searchParams;const period=params.period||"last_30_days";const query=new URLSearchParams({period});if(params.date_from)query.set("date_from",params.date_from);if(params.date_to)query.set("date_to",params.date_to);const data=await serverGet<any>(`/dashboard/owner?${query}`,cookie);return <AppShell user={user} title="Owner dashboard"><OwnerDashboard data={data}/></AppShell>}
