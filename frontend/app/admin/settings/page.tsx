import Link from"next/link";import{cookies}from"next/headers";import{redirect}from"next/navigation";import{AppShell}from"@/components/app-shell";import{getCurrentUser}from"@/lib/api";
export default async function SettingsPage(){const cookie=(await cookies()).toString();const user=await getCurrentUser(cookie);if(!user)redirect("/login");if(user.role!=="owner")redirect("/admin/inventory");return <AppShell user={user} title="Settings"><div className="grid gap-4 md:grid-cols-2"><Link href="/admin/settings/payments" className="rounded-2xl border bg-white p-6 hover:border-leaf-600"><h2 className="font-semibold text-leaf-900">Payment settings</h2><p className="mt-2 text-sm text-slate-500">Manage the centrally controlled business name, UPI ID, bank reference and staff-facing QR.</p></Link><Link href="/admin/staff" className="rounded-2xl border bg-white p-6 hover:border-leaf-600"><h2 className="font-semibold text-leaf-900">Staff access</h2><p className="mt-2 text-sm text-slate-500">Create accounts, change roles, reset passwords and deactivate access.</p></Link></div><section className="mt-6 rounded-2xl border border-dashed bg-white p-6"><h2 className="font-semibold">System configuration</h2><p className="mt-2 text-sm text-slate-500">Company profile and notification preferences are not part of V1. Deployment secrets remain environment-managed.</p></section></AppShell>}
