import{cookies}from"next/headers";import{redirect}from"next/navigation";import{AppShell}from"@/components/app-shell";import{PaymentSettingsForm}from"@/components/payment-settings-form";import{getCurrentUser,serverGet}from"@/lib/api";
export default async function Page(){const cookie=(await cookies()).toString();const user=await getCurrentUser(cookie);if(!user)redirect("/login");if(user.role!=="owner")redirect(user.role==="staff"?"/staff":"/admin/inventory");let settings=null;try{settings=await serverGet<any>("/payments/settings",cookie)}catch{}return <AppShell user={user} title="Payment settings"><p className="mb-6 max-w-2xl text-sm text-slate-500">This QR is centrally managed and shown to staff. Displaying it never changes a sale’s payment status.</p><PaymentSettingsForm settings={settings}/></AppShell>}
