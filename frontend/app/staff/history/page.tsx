import{cookies}from"next/headers";import{redirect}from"next/navigation";import{AppShell}from"@/components/app-shell";import{SalesHistory}from"@/components/sales-history";import{getCurrentUser,serverGet}from"@/lib/api";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string>>}){const cookie=(await cookies()).toString();const user=await getCurrentUser(cookie);if(!user)redirect("/login");if(user.role!=="staff")redirect("/admin/sales");const p=await searchParams;const query=new URLSearchParams(Object.entries(p).filter(([,v])=>v));const rows=await serverGet<any[]>(`/sales?${query}`,cookie);return <AppShell user={user} title="Sales history"><Filters/><SalesHistory rows={rows}/></AppShell>};
function Filters(){return <form className="mb-5 grid gap-3 rounded-2xl border bg-white p-4 sm:grid-cols-3"><input name="date_from" type="date"/><input name="date_to" type="date"/><select name="payment_status" className="rounded-xl border px-3"><option value="">All payments</option><option value="paid">Paid</option><option value="pending">Pending</option></select><button className="rounded-xl bg-leaf-700 px-4 py-2 text-white sm:col-span-3">Apply filters</button></form>}
