"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{API_URL}from"@/lib/api";
export function PaymentReceivedButton({saleId}:{saleId:string}){const router=useRouter();const[method,setMethod]=useState("upi");const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function mark(){if(!window.confirm("Confirm that payment was actually received? This does not verify with the bank."))return;setBusy(true);setError("");try{const res=await fetch(`${API_URL}/payments/sales/${saleId}/received`,{method:"POST",credentials:"include",headers:{"Content-Type":"application/json"},body:JSON.stringify({payment_method:method})});const json=await res.json();if(!res.ok)throw new Error(json.error?.message??"Could not update payment");router.refresh()}catch(err){setError(err instanceof Error?err.message:"Network error")}finally{setBusy(false)}}return <div className="mt-6"><label className="mb-2 block text-xs font-semibold uppercase tracking-wider text-slate-500">Payment received via</label><div className="flex gap-2"><select value={method} onChange={e=>setMethod(e.target.value)} className="min-w-0 flex-1 rounded-xl border px-3"><option value="upi">UPI</option><option value="cash">Cash</option><option value="bank_transfer">Bank transfer</option><option value="credit">Credit</option><option value="other">Other</option></select><button disabled={busy} onClick={mark} className="rounded-xl bg-leaf-700 px-4 py-3 font-semibold text-white">{busy?"Updating…":"Mark received"}</button></div>{error&&<p className="mt-2 text-sm text-red-700">{error}</p>}</div>}
