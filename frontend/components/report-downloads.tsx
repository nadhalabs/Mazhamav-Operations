"use client";import{useState}from"react";import{API_URL}from"@/lib/api";
const reports=[["sales","Sales report","Every sale with retailer, staff and payment status"],["staff-sales","Staff sales report","Sales totals grouped by staff"],["product-sales","Product sales report","Quantity and revenue by product"],["retailer-sales","Retailer sales report","Sales totals grouped by retailer"],["inventory-movements","Inventory movement report","Immutable stock ledger entries"],["stock-requests","Stock request report","Request status and fulfilment audit"],["pending-payments","Pending payment report","Outstanding sale payments"]];
export function ReportDownloads(){const[from,setFrom]=useState("");const[to,setTo]=useState("");function href(id:string){const q=new URLSearchParams();if(from)q.set("date_from",from);if(to)q.set("date_to",to);return`${API_URL}/reports/${id}.csv?${q}`}return <><div className="mb-6 grid gap-3 rounded-2xl border bg-white p-4 sm:grid-cols-2"><label className="text-sm font-medium">From<input className="mt-1 w-full" type="date" value={from} onChange={e=>setFrom(e.target.value)}/></label><label className="text-sm font-medium">To<input className="mt-1 w-full" type="date" value={to} min={from} onChange={e=>setTo(e.target.value)}/></label></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{reports.map(([id,title,description])=><article key={id} className="flex flex-col rounded-2xl border bg-white p-5"><h2 className="font-semibold">{title}</h2><p className="mt-2 flex-1 text-sm text-slate-500">{description}</p><a href={href(id)} className="mt-5 rounded-xl border border-leaf-700 px-4 py-2 text-center text-sm font-semibold text-leaf-700 hover:bg-leaf-50">Download CSV</a></article>)}</div></>}
