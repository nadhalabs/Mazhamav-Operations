"use client";import Link from"next/link";import{FormEvent,useState}from"react";import{API_URL,apiJson}from"@/lib/api";
type Product={id:string;name:string;sku:string;unit_name:string;selling_price:string;available:string};type Retailer={id:string;shop_name:string;area?:string;district?:string};type SaleResult={id:string;sale_number:string;total:string;payment_status:string};
export function RecordSaleForm({products,retailers}:{products:Product[];retailers:Retailer[]}){const[retailer,setRetailer]=useState(""),[newRetailer,setNewRetailer]=useState(false),[query,setQuery]=useState(""),[quantities,setQuantities]=useState<Record<string,number>>({}),[status,setStatus]=useState("paid"),[method,setMethod]=useState("cash"),[busy,setBusy]=useState(false),[result,setResult]=useState<SaleResult|null>(null),[error,setError]=useState("");const shown=retailers.filter(r=>`${r.shop_name} ${r.area||""} ${r.district||""}`.toLowerCase().includes(query.toLowerCase())).slice(0,12),selected=products.filter(p=>(quantities[p.id]||0)>0),total=selected.reduce((sum,p)=>sum+Number(p.selling_price)*quantities[p.id],0);function setQty(p:Product,value:number){setQuantities(q=>({...q,[p.id]:Math.max(0,Math.min(Number(p.available),value))}))}async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!confirm(`Confirm sale for ₹${total.toFixed(2)}? Stock will be deducted immediately.`))return;setBusy(true);setError("");setResult(null);const fd=new FormData(e.currentTarget),body={retailer_id:newRetailer?null:retailer,new_retailer:newRetailer?{shop_name:fd.get("shop_name"),contact_name:null,phone:fd.get("retailer_phone")||null,area:fd.get("area")||null,district:null,address:null}:null,items:selected.map(p=>({product_id:p.id,quantity:quantities[p.id]})),payment_status:status,payment_method:status==="paid"?method:null,notes:fd.get("notes")||null,idempotency_key:crypto.randomUUID()};try{const res=await fetch(`${API_URL}/sales`,{method:"POST",credentials:"include",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const sale=await apiJson<SaleResult>(res,"Sale could not be recorded.");setResult(sale);setQuantities({});setRetailer("")}catch(err){setError(err instanceof Error?err.message:"Sale could not be recorded.")}finally{setBusy(false)}}return <form onSubmit={submit} className="mx-auto max-w-3xl space-y-5"><Step number="1" title="Retailer"><div className="flex justify-end"><button type="button" onClick={()=>{setNewRetailer(x=>!x);setRetailer("")}} className="text-sm font-semibold text-leaf-700">{newRetailer?"Choose existing":"+ Add new retailer"}</button></div>{newRetailer?<div className="mt-3 grid gap-3 sm:grid-cols-3"><input name="shop_name" required placeholder="Shop name"/><input name="retailer_phone" placeholder="Phone"/><input name="area" placeholder="Area"/></div>:<><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search shop or area" className="mt-3"/><div className="mt-3 flex gap-2 overflow-x-auto pb-1">{shown.map(r=><button type="button" key={r.id} onClick={()=>setRetailer(r.id)} className={`min-w-40 rounded-2xl p-3 text-left text-sm ring-1 ${retailer===r.id?"bg-leaf-50 ring-leaf-600":"bg-white ring-slate-200"}`}><b>{r.shop_name}</b><span className="mt-1 block text-slate-500">{r.area||"Retailer"}</span></button>)}</div></>}</Step><Step number="2–3" title="Products & quantity"><div className="mt-4 space-y-3">{products.map(p=>{const qty=quantities[p.id]||0,{name,pack}=splitPack(p.name);return <article key={p.id} className={`rounded-2xl p-4 ring-1 ${qty?"bg-leaf-50 ring-leaf-500":"bg-white ring-slate-200"}`}><div className="flex items-start justify-between gap-3"><div><p className="font-semibold">{name}</p><p className="text-sm font-bold text-leaf-800">{pack||p.sku}</p><p className="mt-1 text-sm text-slate-500">₹{p.selling_price} · {p.available} {p.unit_name}s left</p></div><div className="flex items-center gap-2"><button type="button" aria-label={`Remove one ${p.name}`} onClick={()=>setQty(p,qty-1)} className="h-11 w-11 rounded-xl bg-white text-xl shadow-sm ring-1 ring-slate-200">−</button><input aria-label={`${p.name} quantity`} type="number" min="0" max={p.available} value={qty} onChange={e=>setQty(p,Number(e.target.value))} className="h-11 w-16 p-1 text-center text-lg font-bold"/><button type="button" aria-label={`Add one ${p.name}`} onClick={()=>setQty(p,qty+1)} className="h-11 w-11 rounded-xl bg-leaf-700 text-xl text-white">+</button></div></div></article>})}</div></Step><Step number="4" title="Payment"><div className="mt-3 grid grid-cols-2 gap-3"><button type="button" onClick={()=>setStatus("paid")} className={`rounded-2xl p-4 font-semibold ring-1 ${status==="paid"?"bg-leaf-50 ring-leaf-600":"ring-slate-200"}`}>Paid</button><button type="button" onClick={()=>setStatus("pending")} className={`rounded-2xl p-4 font-semibold ring-1 ${status==="pending"?"bg-amber-50 ring-amber-500":"ring-slate-200"}`}>Pending</button></div>{status==="paid"&&<select value={method} onChange={e=>setMethod(e.target.value)} className="mt-3 w-full"><option value="cash">Cash</option><option value="upi">UPI</option><option value="bank_transfer">Bank transfer</option><option value="credit">Credit</option><option value="other">Other</option></select>}<textarea name="notes" placeholder="Optional note" className="mt-3 w-full"/></Step><section className="sticky bottom-20 z-20 rounded-2xl bg-slate-950 p-4 text-white shadow-xl"><div className="flex items-center justify-between"><div><p className="text-xs text-slate-300">{selected.length} product{selected.length===1?"":"s"}</p><p className="text-2xl font-bold">₹{total.toFixed(2)}</p></div><button disabled={busy||(!retailer&&!newRetailer)||!selected.length} className="min-h-12 rounded-xl bg-leaf-500 px-6 font-bold disabled:opacity-40">{busy?"Recording…":"5. Confirm Sale"}</button></div></section>{error&&<p role="alert" className="rounded-2xl bg-red-50 p-4 text-red-700">{error}</p>}{result&&<div className="rounded-2xl bg-emerald-50 p-5 text-emerald-900"><p className="font-bold">Sale {result.sale_number} recorded</p><p>₹{result.total}</p><div className="mt-3 flex gap-3">{result.payment_status==="pending"&&<Link href={`/staff/payment-qr?sale_id=${result.id}`} className="font-semibold underline">Show payment QR</Link>}<Link href={`/staff/history/${result.id}`} className="font-semibold underline">View sale</Link></div></div>}</form>}
function Step({number,title,children}:{number:string;title:string;children:React.ReactNode}){return <section className="rounded-3xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><h2 className="font-semibold"><span className="mr-2 text-leaf-700">{number}.</span>{title}</h2>{children}</section>}
function splitPack(value:string){const match=value.match(/^(.*?)(\d+(?:\.\d+)?\s*(?:g|kg|ml|l))$/i);return match?{name:match[1].trim(),pack:match[2]}:{name:value,pack:""}}
