"use client";import{useRouter}from"next/navigation";import{API_URL}from"@/lib/api";
type Row={id:string;staff_name:string;product_name:string;unit_name:string;requested_quantity:string;current_staff_balance:string;requested_at:string;status:string;notes?:string;fulfilled_quantity?:string;is_low_stock:boolean;is_old_pending:boolean};
export function StockRequestAdmin({rows}:{rows:Row[]}){const router=useRouter();async function action(id:string,name:"approve"|"reject"|"fulfil",quantity?:string){const note=name==="reject"?window.prompt("Reason for rejection (optional)"):null;const body=name==="fulfil"?{fulfilled_quantity:quantity||null}:{note};const res=await fetch(`${API_URL}/stock-requests/${id}/${name}`,{method:"POST",credentials:"include",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const json=await res.json();if(!res.ok)window.alert(json.error?.message??"Action failed");else router.refresh()}return <div className="space-y-3">{rows.map(r=><article key={r.id} className={`rounded-2xl border bg-white p-5 ${r.is_old_pending?"border-amber-400":""}`}><div className="grid gap-4 lg:grid-cols-[1.3fr_1fr_1fr_auto]"><div><div className="flex flex-wrap items-center gap-2"><b>{r.staff_name}</b>{r.is_low_stock&&<span className="rounded-full bg-red-50 px-2 py-1 text-xs font-semibold text-red-700">Low stock</span>}{r.is_old_pending&&<span className="rounded-full bg-amber-50 px-2 py-1 text-xs font-semibold text-amber-800">Old pending</span>}</div><p className="text-sm text-slate-500">{r.product_name} · {r.requested_quantity} {r.unit_name}</p>{r.notes&&<p className="mt-2 text-sm text-slate-500">“{r.notes}”</p>}</div><div><p className="text-xs text-slate-400">Current staff balance</p><p className="mt-1 text-xl font-bold">{r.current_staff_balance}</p></div><div><p className="text-xs text-slate-400">Requested</p><p className="mt-1 text-sm">{new Date(r.requested_at).toLocaleString()}</p><span className="mt-2 inline-block rounded-full bg-slate-100 px-2 py-1 text-xs font-semibold capitalize">{r.status}</span></div><div className="flex min-w-48 flex-col gap-2">{r.status==="pending"&&<><button onClick={()=>action(r.id,"approve")} className="rounded-lg border border-blue-600 px-3 py-2 text-sm text-blue-700">Approve</button><button onClick={()=>action(r.id,"reject")} className="rounded-lg border border-red-600 px-3 py-2 text-sm text-red-700">Reject</button></>}{["pending","approved"].includes(r.status)&&<Fulfil requested={r.requested_quantity} onSubmit={q=>action(r.id,"fulfil",q)}/>} {r.fulfilled_quantity&&<p className="text-sm text-green-700">Fulfilled: {r.fulfilled_quantity}</p>}</div></div></article>)}</div>}
function Fulfil({requested,onSubmit}:{requested:string;onSubmit:(quantity:string)=>void}){return <form onSubmit={e=>{e.preventDefault();onSubmit(new FormData(e.currentTarget).get("quantity")?.toString()??requested)}} className="flex gap-2"><input name="quantity" type="number" min="0.001" step="0.001" defaultValue={requested} className="min-w-0 px-2 py-2"/><button className="rounded-lg bg-leaf-700 px-3 py-2 text-sm text-white">Fulfil</button></form>}
